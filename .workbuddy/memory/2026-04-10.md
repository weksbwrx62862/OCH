# 2026-04-10 工作日志

## 技术债修复（全部 7 项完成）

### P0 修复
1. **async_utils.py 引擎复用**：模块级单例引擎 + `threading.Lock` 双重检查，`get_db()` 只创建 session；添加 `dispose_engine()` + `atexit` 关闭钩子
2. **认证统一**：AuthMiddleware 为唯一认证入口，直接写 `flask.g.user`；`init_security()` 移除 `before_request`；main.py `_run_middleware_sync` 同步写入 `g.user`
3. **信息泄露修复**：`handle_generic_error` 和 `bad_request` handler 添加环境判断，生产环境隐藏异常详情

### P1 修复
4. **datetime.utcnow() 全面替换**：25 个文件 65 处 → `datetime.now(timezone.utc)`，自动添加 `timezone` 导入
5. **前端 chat/page.tsx 拆分**：619 行 → 5 个文件（types.ts, MessageBubble.tsx, MemorySidebar.tsx, ChatInput.tsx, page.tsx）
6. **CoordinatorService 数据库持久化**：纯内存 `_teams` 字典 → Team/TeamMember 数据库模型

### 验证
- 46 个 Python 文件编译通过
- TypeScript 编译通过
- 16/16 项自动化验证全部 PASS
- 无 `datetime.utcnow()` 残留
