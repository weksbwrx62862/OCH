{
  "name": "browser_hover",
  "description": "Hover over element on page.\nIMPORTANT: This triggers JavaScript mouse events (mouseover, mouseenter, etc.) only.\nIt does NOT trigger CSS :hover pseudo-class styles. If a UI element relies purely on CSS :hover for visual changes, those changes will NOT appear after this action.\nUse this tool when the page uses JavaScript event listeners to show/hide content on hover.\n",
  "arguments": {
    "type": "object",
    "properties": {
      "element": {
        "description": "Human-readable element description used to obtain permission to interact with the element",
        "type": "string"
      },
      "ref": {
        "description": "Exact target element reference from the page snapshot",
        "type": "string"
      },
      "viewId": {
        "description": "Target browser tab ID. If omitted, uses the last interacted tab.",
        "type": "string"
      }
    },
    "required": [
      "element",
      "ref"
    ]
  }
}