# edict项目 - 重新安装和启动计划

## [x] 任务1: 停止当前运行的edict相关进程
- **优先级**: P0
- **依赖**: 无
- **描述**:
  - 检查并停止所有与edict相关的进程，包括数据刷新循环和看板服务器
  - 确保没有进程占用相关端口
- **成功标准**:
  - 所有edict相关进程已停止
  - 7891端口（看板服务器）已释放
- **测试要求**:
  - `programmatic` TR-1.1: 运行 `ps aux | grep -E 'run_loop|dashboard/server'` 无相关进程
  - `programmatic` TR-1.2: 运行 `lsof -i :7891` 无输出
- **备注**: 使用kill命令停止进程时要小心，避免误杀其他进程

## [ ] 任务2: 清理旧的安装数据（可选）
- **优先级**: P1
- **依赖**: 任务1
- **描述**:
  - 清理旧的Agent Workspace数据
  - 备份重要配置文件
- **成功标准**:
  - 旧的Workspace数据已清理或备份
- **测试要求**:
  - `programmatic` TR-2.1: 检查 `~/.openclaw/workspace-*` 目录状态
- **备注**: 如需保留历史数据，可跳过此步骤

## [x] 任务3: 重新运行安装脚本
- **优先级**: P0
- **依赖**: 任务1
- **描述**:
  - 运行 install.sh 脚本重新安装edict项目
  - 确保所有依赖已满足
- **成功标准**:
  - 安装脚本执行完成，无错误
  - 所有Agent已注册
- **测试要求**:
  - `programmatic` TR-3.1: 安装脚本执行完成，退出码为0
  - `programmatic` TR-3.2: 检查 `~/.openclaw/openclaw.json` 中包含edict相关Agent配置
- **备注**: 安装过程中可能需要输入确认信息

## [x] 任务4: 启动数据刷新循环
- **优先级**: P0
- **依赖**: 任务3
- **描述**:
  - 启动后台数据刷新循环进程
- **成功标准**:
  - 数据刷新循环进程已在后台运行
- **测试要求**:
  - `programmatic` TR-4.1: 运行 `ps aux | grep run_loop` 显示进程在运行
  - `programmatic` TR-4.2: 检查 `edict/run_loop.log` 文件有最新日志
- **备注**: 使用 `&` 符号在后台运行

## [x] 任务5: 启动看板服务器
- **优先级**: P0
- **依赖**: 任务3
- **描述**:
  - 启动看板服务器
- **成功标准**:
  - 看板服务器已启动，监听7891端口
- **测试要求**:
  - `programmatic` TR-5.1: 运行 `lsof -i :7891` 显示服务器在监听
  - `programmatic` TR-5.2: 访问 http://127.0.0.1:7891 能正常打开
- **备注**: 可使用 `nohup` 命令让服务器在后台运行

## [x] 任务6: 验证系统状态
- **优先级**: P1
- **依赖**: 任务4, 任务5
- **描述**:
  - 验证所有服务是否正常运行
  - 检查Agent状态
  - 验证数据同步是否正常
- **成功标准**:
  - 所有服务运行正常
  - Agent状态显示在线
  - 数据同步正常
- **测试要求**:
  - `programmatic` TR-6.1: 检查 `edict/data/live_status.json` 显示正常状态
  - `human-judgement` TR-6.2: 看板界面显示正常，无错误信息
- **备注**: 可通过看板界面验证系统状态

## [x] 任务7: 故障排查（如有需要）
- **优先级**: P2
- **依赖**: 任务6
- **描述**:
  - 如遇到问题，进行故障排查
  - 查看日志文件，分析错误原因
  - 采取相应的修复措施
- **成功标准**:
  - 所有问题已解决
  - 系统正常运行
- **测试要求**:
  - `programmatic` TR-7.1: 所有服务正常运行
  - `human-judgement` TR-7.2: 系统功能正常，无错误
- **备注**: 常见问题包括端口占用、依赖缺失等